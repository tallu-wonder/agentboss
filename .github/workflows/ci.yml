name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          check-latest: true

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "not gofmt'd:"; echo "$unformatted"; exit 1
          fi

      - name: vet
        run: go vet ./...

      # tmux-backed tests skip themselves when tmux is missing; install it so
      # they actually run.
      - name: install tmux
        run: |
          if [ "$RUNNER_OS" = "macOS" ]; then brew install tmux; else sudo apt-get update && sudo apt-get install -y tmux; fi

      # tmux's option set varies by version, and the desk-level tests care.
      - name: tmux version
        run: tmux -V

      - name: test
        run: go test -race ./...

      # Drives a real manager in a private tmux server: the desk-level
      # regressions (tabs, digit keys, confirmations, notifications) only show
      # up here.
      - name: end-to-end
        run: go test -tags e2e -timeout 10m ./internal/e2e/

      - name: build
        run: go build ./...

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          # The module's own toolchain, NOT stable: the day a new Go ships,
          # staticcheck's latest release can't read its export data yet, and
          # lint would break with zero changes here.
          go-version-file: go.mod
      - name: staticcheck
        run: |
          go install honnef.co/go/tools/cmd/staticcheck@latest
          staticcheck ./...
      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...
